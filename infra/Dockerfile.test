FROM python:3.11-slim

WORKDIR /app
COPY python/ /app/python/

# Install package with test dependencies
RUN pip install --no-cache-dir /app/python[test]

ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app/python

# Default to running tests
CMD ["pytest", "-v"]
